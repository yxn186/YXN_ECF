# YXN_ECF 使用指南

## 1. 项目说明

`YXN_ECF` 是一个独立的嵌入式公共代码仓库，用于在多个 STM32 主控工程之间复用代码。

当前主要服务于以下两个主控工程：

```text
SteeringWheel_Infantry_Gimbal
SteeringWheel_Infantry_Chassis
```

两个主控工程均为独立 Git 仓库，并在项目根目录中通过 Git Submodule 引入 `YXN_ECF`。

---

## 2. 当前仓库关系

本地目录结构：

```text
SteeringWheel_Infantry/
├── SteeringWheel_Infantry_Gimbal/
│   ├── .git/
│   ├── .gitmodules
│   ├── YXN_ECF/                  # Git Submodule
│   ├── Core/
│   ├── Drivers/
│   ├── CMakeLists.txt
│   └── ...
│
├── SteeringWheel_Infantry_Chassis/
│   ├── .git/
│   ├── .gitmodules
│   ├── YXN_ECF/                  # Git Submodule
│   ├── Core/
│   ├── Drivers/
│   ├── CMakeLists.txt
│   └── ...
│
└── YXN_ECF/
    ├── .git/
    ├── README.md
    └── USAGE.md
```

对应三个独立 Git 仓库：

```text
SteeringWheel_Infantry_Gimbal
SteeringWheel_Infantry_Chassis
YXN_ECF
```

主控仓库不会直接保存 `YXN_ECF` 的全部源码，而是记录：

```text
当前主控使用的是 YXN_ECF 的哪一个 Git 提交
```

---

## 3. VS Code 中的仓库显示

使用 VS Code 打开主控工程后，源代码管理页面中通常会显示两个仓库：

```text
SteeringWheel_Infantry_Gimbal
└── YXN_ECF
```

或者：

```text
SteeringWheel_Infantry_Chassis
└── YXN_ECF
```

它们是两个独立的 Git 仓库。

### 选中主控仓库

用于提交：

- 主控代码
- CubeMX 配置
- CMake 配置
- FreeRTOS 任务
- `YXN_ECF` 的版本指针

### 选中 YXN_ECF 仓库

用于提交：

- 公共算法
- 公共驱动
- 公共通信协议
- 公共工具代码
- YXN_ECF 文档

在执行提交、拉取或推送前，必须先确认当前选中的是哪个仓库。

---

## 4. 第一次克隆主控工程

推荐使用以下命令克隆主控工程：

```bash
git clone --recurse-submodules <主控仓库地址>
```

例如：

```bash
git clone --recurse-submodules <云台仓库地址>
```

`--recurse-submodules` 会同时下载主控工程和其指定版本的 `YXN_ECF`。

如果已经普通克隆了主控工程，而 `YXN_ECF` 文件夹为空，在主控根目录执行：

```bash
git submodule update --init --recursive
```

---

## 5. 将 YXN_ECF 接入新的主控工程

在新的主控工程根目录执行：

```bash
git submodule add <YXN_ECF仓库地址> YXN_ECF
```

完成后会生成：

```text
.gitmodules
YXN_ECF/
```

然后提交主控仓库：

```bash
git add .gitmodules YXN_ECF
git commit -m "Add YXN_ECF submodule"
git push
```

子模块直接放在主控项目根目录，不使用额外的 `Libraries` 文件夹。

---

## 6. 在主控工程中修改 YXN_ECF

可以直接在云台或底盘工程里的 `YXN_ECF` 文件夹中修改公共代码。

例如：

```text
SteeringWheel_Infantry_Gimbal/
└── YXN_ECF/
    └── BMI088/
        ├── BMI088.h
        └── BMI088.cpp
```

### 第一步：选中 YXN_ECF 仓库

在 VS Code 的“源代码管理”中点击：

```text
YXN_ECF
```

确认当前分支为：

```text
main
```

修改前建议先点击：

```text
YXN_ECF 右侧 ...
→ 拉取
```

避免在旧版本上继续开发。

### 第二步：修改公共代码

添加或修改对应文件，例如：

```text
BMI088/BMI088.h
BMI088/BMI088.cpp
```

### 第三步：提交 YXN_ECF

保持选中 `YXN_ECF` 仓库：

1. 暂存需要提交的文件；
2. 填写提交信息；
3. 点击“提交”；
4. 点击“推送”或“同步更改”。

例如提交信息：

```text
Add BMI088 driver
```

这次提交会进入 `YXN_ECF` 自己的 GitHub 仓库。

---

## 7. 提交主控中的 YXN_ECF 版本变化

当 `YXN_ECF` 完成一次新提交后，主控仓库会显示：

```text
YXN_ECF    M
```

这里的 `M` 不是说主控仓库直接修改了公共库源码，而是表示：

```text
主控记录的 YXN_ECF 提交版本发生了变化
```

接下来：

1. 在源代码管理中选中主控仓库；
2. 暂存 `YXN_ECF`；
3. 填写提交信息；
4. 提交并推送主控仓库。

推荐提交信息：

```text
Update YXN_ECF dependency
```

因此，一次公共库修改通常需要两次提交：

```text
第一次：提交 YXN_ECF 的源码变化
第二次：提交主控仓库中的 YXN_ECF 版本指针
```

---

## 8. 让另一个主控使用新版 YXN_ECF

假设已经在云台工程中修改并推送了新版 `YXN_ECF`，现在底盘也需要使用新版。

### 第一步：打开底盘工程

使用 VS Code 单独打开：

```text
SteeringWheel_Infantry_Chassis
```

### 第二步：选中底盘里的 YXN_ECF

在源代码管理中点击：

```text
YXN_ECF
```

确认它处于：

```text
main
```

然后点击：

```text
YXN_ECF 右侧 ...
→ 拉取
```

也可以在底盘工程的 `YXN_ECF` 目录中执行：

```bash
git switch main
git pull
```

### 第三步：测试底盘工程

重新进行：

- CMake Configure
- Build
- 下载测试
- 功能测试

确认新版公共库不会导致底盘工程异常。

### 第四步：提交底盘的版本指针

选中底盘主仓库，会看到：

```text
YXN_ECF    M
```

暂存并提交：

```text
Update YXN_ECF dependency
```

然后推送底盘仓库。

---

## 9. 拉取主控工程更新

在主控仓库中执行普通 `git pull` 时，Git 只会更新：

```text
主控代码
主控记录的 YXN_ECF 提交编号
```

本地子模块不一定会自动切换到该提交。

因此拉取主控工程后，推荐执行：

```bash
git pull
git submodule update --init --recursive
```

含义如下：

```text
git pull
    更新主控仓库

git submodule update --init --recursive
    将 YXN_ECF 切换到主控仓库记录的指定版本
```

这和主动升级到 `YXN_ECF/main` 最新版本不同。

### 恢复到主控指定版本

```bash
git submodule update --init --recursive
```

### 主动升级到 YXN_ECF 最新 main

进入子模块后执行：

```bash
git switch main
git pull
```

升级完成后，还需要在主控仓库中提交新的子模块版本指针。

---

## 10. 分离头指针说明

重新克隆工程或执行子模块更新后，`YXN_ECF` 可能处于 Detached HEAD 状态。

此时 VS Code 分支位置可能显示：

```text
一串提交号
```

而不是：

```text
main
```

在修改 `YXN_ECF` 前，应先切换到 `main`：

```bash
cd YXN_ECF
git switch main
git pull
```

也可以在 VS Code 中：

1. 选中 `YXN_ECF` 仓库；
2. 点击当前分支名称；
3. 选择 `main`；
4. 执行拉取。

不要在 Detached HEAD 状态下直接提交长期保留的代码。

---

## 11. 推荐的日常开发流程

### 修改主控专用代码

例如修改云台控制任务：

```text
SteeringWheel_Infantry_Gimbal/Usercode/
```

只需要：

1. 选中云台主仓库；
2. 暂存修改；
3. 提交；
4. 推送。

不需要操作 `YXN_ECF`。

### 修改公共代码

例如修改 PID、BMI088 或板间通信协议：

1. 选中 `YXN_ECF`；
2. 拉取最新代码；
3. 修改并测试；
4. 提交并推送 `YXN_ECF`；
5. 选中当前主控仓库；
6. 提交新的 `YXN_ECF` 版本指针；
7. 在另一个主控中拉取新版 `YXN_ECF`；
8. 测试后提交另一个主控的版本指针。

---

## 12. 推荐的提交信息

### YXN_ECF 源码提交

```text
Add BMI088 driver
Add board communication protocol
Fix PID integral limiting
Improve FDCAN message parsing
Refactor DJI motor module
Update YXN_ECF documentation
```

### 主控更新子模块版本

```text
Update YXN_ECF dependency
Update YXN_ECF for BMI088 driver
Update board communication dependency
```

### 添加子模块

```text
Add YXN_ECF submodule
```

---

## 13. YXN_ECF 代码存放原则

适合放入 `YXN_ECF`：

```text
PID
滤波算法
CRC
数学工具
环形缓冲区
板间通信协议
通用电机类
通用传感器驱动
公共状态枚举
公共数据结构
```

不适合放入 `YXN_ECF`：

```text
main.c
freertos.c
gpio.c
tim.c
fdcan.c
dma.c
stm32h7xx_it.c
具体板卡引脚配置
具体外设初始化
云台任务
底盘任务
机器人具体应用逻辑
```

判断标准：

> 一份代码能否在不同主控中不修改源码，只通过构造参数、配置或外设句柄完成复用。

可以复用，适合放入 `YXN_ECF`。

明显依赖某块主控的具体硬件配置，则留在对应主控工程。

---

## 14. YXN_ECF 目录建议

每个独立库使用一个文件夹：

```text
YXN_ECF/
├── BMI088/
│   ├── BMI088.h
│   └── BMI088.cpp
│
├── PID/
│   ├── PID.h
│   └── PID.cpp
│
├── DJI_Motor/
│   ├── DJI_Motor.h
│   └── DJI_Motor.cpp
│
├── AS5600/
│   ├── AS5600.h
│   └── AS5600.cpp
│
├── Board_Communication/
│   ├── Board_Communication.h
│   └── Board_Communication.cpp
│
├── README.md
└── USAGE.md
```

`YXN_ECF` 本身不负责生成完整 STM32 固件，不包含 CubeMX 工程。

具体主控需要使用哪个库，由主控自己的 `CMakeLists.txt` 决定。

---

## 15. 常见问题

### YXN_ECF 文件夹为空

执行：

```bash
git submodule update --init --recursive
```

### 修改了 YXN_ECF，但主控仓库显示 YXN_ECF 被修改

这是正常现象。

表示主控记录的子模块提交版本发生了变化，需要在主控仓库中提交新的版本指针。

### 在云台中更新了 YXN_ECF，底盘没有自动更新

这是正常现象。

Submodule 会锁定具体提交，底盘必须在自己的 `YXN_ECF` 中主动拉取新版，然后提交底盘使用的新版本指针。

### 拉取主控后，YXN_ECF 仍然不是正确版本

执行：

```bash
git submodule update --init --recursive
```

### VS Code 中提交到了错误的仓库

提交前检查源代码管理顶部选中的仓库名称：

```text
YXN_ECF
```

表示提交公共库。

```text
SteeringWheel_Infantry_Gimbal
```

表示提交云台主控。

```text
SteeringWheel_Infantry_Chassis
```

表示提交底盘主控。

---

## 16. 快速操作表

| 目的 | 操作仓库 | 操作 |
|---|---|---|
| 修改公共库源码 | YXN_ECF | 拉取、修改、提交、推送 |
| 修改云台代码 | Gimbal | 修改、提交、推送 |
| 修改底盘代码 | Chassis | 修改、提交、推送 |
| 云台采用新版公共库 | Gimbal 内的 YXN_ECF | 拉取，然后提交 Gimbal 的子模块指针 |
| 底盘采用新版公共库 | Chassis 内的 YXN_ECF | 拉取，然后提交 Chassis 的子模块指针 |
| 恢复主控指定的公共库版本 | 主控根目录 | `git submodule update --init --recursive` |
| 克隆完整主控项目 | 终端 | `git clone --recurse-submodules <仓库地址>` |

---

## 17. 核心理解

```text
YXN_ECF 仓库
    保存公共库真正的源码和提交历史

主控仓库
    保存主控源码
    并记录当前使用的 YXN_ECF 提交版本
```

因此，公共库修改后的标准流程是：

```text
提交 YXN_ECF 源码
        ↓
提交当前主控的 YXN_ECF 版本指针
        ↓
另一个主控拉取新版 YXN_ECF
        ↓
提交另一个主控的 YXN_ECF 版本指针
```